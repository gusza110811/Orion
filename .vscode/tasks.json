{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run script",
            "command":"lua",
            "args": [
                "${fileDirname}/${fileBasename}"
            ],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}